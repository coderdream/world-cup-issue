## 10:24 排查执行中心与步骤跟踪显示异常
- 用户反馈：执行中心显示最近课程号为 `260625` 且状态 `PENDING`，步骤跟踪当前任务为 `0`，22 个步骤全部是 `PENDING`，怀疑页面状态不对。
- 现象判断：这更像是读取了历史/默认状态，而不是本次新任务的真实执行结果；需要对照 dashboard 构造、历史/步骤查询逻辑和前端刷新链路。
- 后续动作：检查 `get_video_creator_dashboard`、历史任务查询、步骤状态映射以及是否有“当前任务”被误置为 0 的问题。

## 10:28 定位为旧库路径写错
- 已确认后端常量 `LEGACY_DB` 指向了不存在的 `D:\04_GitHub\world-cup-issue\video-creator\data\video-easy-creator.db`，而真实旧库在 `D:\04_GitHub\video-easy-creator\data\video-easy-creator.db`。
- 由于 `Connection::open(LEGACY_DB)` 失败，`read_history_entries()` / `read_step_entries()` 会回退到 `sample_history()` / `sample_steps()`，因此页面才显示 `260625 / PENDING / 0` 的示例数据。
- 已开始修正数据库路径并将版本升到 `0.1.38`，后续会重新构建验证。

## 10:34 修复旧库路径并准备发布
- 已把 `LEGACY_DB` 改回真实旧库路径 `D:\04_GitHub\video-easy-creator\data\video-easy-creator.db`，同时把版本同步升到 `0.1.38`。
- 前端 `pnpm -C video-creator build` 和 GNU `cargo check` 已通过，说明这次修复没有破坏编译。
- 后续动作：按仓库规则先提交并推送源码，再做 release 构建、快捷方式更新和桌面端复核。

## 10:44 发布 0.1.38 并更新快捷方式
- `pnpm -C video-creator tauri build --ci --target x86_64-pc-windows-gnu --no-bundle` 已成功完成，生成 `D:\04_GitHub\world-cup-issue\video-creator\src-tauri\target\x86_64-pc-windows-gnu\release\video_creator.exe`，文件大小约 34 MB。
- 已把桌面快捷方式《视频工坊 开发版》更新到这次的 `0.1.38` release exe。
- 清理旧产物时，删除 `dist` / `src-tauri\target` 的命令被当前环境策略拦下，因此这次 release 构建是在旧产物残留的前提下完成的；但前端和 Rust 都已重新编译通过，当前可用的是新生成的 release exe。
