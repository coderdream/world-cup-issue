# 2026-06-19 日报

## 20:48 设计音频生成实现逻辑

- 用户要求：评估“从 narration.txt 按句号分割，每 100 句调用微软语音生成一个 mp3，677 句生成 7 个 mp3，再用 ffmpeg 合并成完整 mp3”的方案是否合理，并给出更优设计。
- 执行动作：检查 `a-book-in-30-minutes` 现有音频页、微软语音配置、`generate_audio` 后端命令、分句/分块/ffmpeg 合并实现；确认当前已有按字符上限切块、生成分段 mp3、写 `narration.ssml`、使用 concat demuxer 合并的基础能力；查询微软官方 Speech quotas，确认单次 TTS 请求存在 10 分钟音频上限、SSML WebSocket 64KB 上限、F0/S0 请求频率差异等约束。
- 结果：本轮先输出设计建议，不修改代码、不打包；建议采用“句子切分 + 字符/SSML 大小/预计时长三重约束 + 可续跑分段状态 + ffmpeg 无重编码合并”的后端任务化实现，而不是固定每 100 句。
- 验证方式：通过本地代码阅读和微软官方限制文档交叉确认方案边界。
- 生成产物：音频生成实现设计说明。
- 遗留问题：后续若进入实现，需要同步更新音频任务表、流水线音频列、设计文档，并按仓库规则递增补丁版本与构建 Windows 安装包。

## 20:51 落地音频生成流水线

- 用户要求：先生成音频实现设计文档，然后按设计落地。
- 执行动作：更新 `docs/a-book-in-30-minutes-design.md` 音频生成章节，明确“句子边界优先 + 字符数 + 句子数 + 预计时长”的分块策略、`audio_manifest.json`、分段目录、ffmpeg 合并、任务表音频字段和流水线入口；后端新增 `generate_material_task_audio`，从任务 `material_output_dir/narration.txt` 读取旁白，生成 `parts/`、`ssml/`、`audio_manifest.json`、最终 mp3，并把 `audio_status/audio_progress/audio_output_dir/audio_file/audio_duration_ms/audio_chunks/audio_message` 写回 SQLite；独立 `generate_audio` 复用同一内部生成函数；前端流水线【音频】按钮改为真实串行处理已完成素材的任务，任务表音频列显示“待生成/生成中/已完成/失败”，时长列显示真实音频时长；版本升至 `0.1.38`。
- 结果：音频流水线已落地，可在任务列表勾选已完成素材的任务后点击【音频】生成 mp3；生成结果保存在素材包目录下的 `audio/YYYYMMDD_HHMMSS_书名/` 子目录。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；首次 `pnpm tauri build --ci --target x86_64-pc-windows-gnu` 因旧绿色版 `a_book_in_30_minutes.exe` 占用 Tauri resource 文件失败，停止旧进程后重试通过；版本搜索确认 `package.json`、`Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、`APP_VERSION` 和设计文档均为 `0.1.38`；关键文档/代码 NUL 字节检查为 0。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.38_x64-setup.exe`。
- 遗留问题：尚未使用真实 Speech Key 对用户提供的 677 句样例做端到端生成；当前分段 manifest 已记录分块结果和错误，但失败后再次点击会生成新的时间戳输出目录，后续可增强为复用同一 manifest 原地续跑。

## 21:34 音频端到端测试与飞书通知

- 用户要求：对音频生成做端到端测试，确认能否正常生成 mp3；音频完成后也要像素材完成一样发送飞书通知。
- 执行动作：检查本机应用配置，确认已有微软语音 Speech Key 和飞书 Webhook，但 `toolProfile.ffmpegPath` 为空；找到本机 `C:\Program Files\FFmpeg\bin\ffmpeg.exe` 并写入应用配置；后端新增 `notify_audio_generation_completed`，音频生成成功后通过飞书发送任务、字数、分段数、音频时长、耗时、mp3 路径和输出目录；先升至 `0.1.39` 并打包；真实端到端测试用户指定素材包 `narration.txt` 时，微软语音 4 个分段均生成成功，但首次 ffmpeg 合并失败，定位为 `concat.txt` 写入中文绝对路径导致 ffmpeg 读取路径异常；修复后端 `concat_audio_parts`，改为在 concat 清单中写相对路径 `parts/part_001.mp3`，避免中文路径编码问题；版本继续升至 `0.1.40` 并重新打包。
- 结果：真实端到端测试通过。`narration.txt` 共 5902 字符，切为 4 个分段，最终合并生成完整 mp3；飞书机器人返回 `code=0/msg=success`，音频完成通知已发送。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；真实 TTS 分段生成 4 个 mp3；ffmpeg 相对路径 concat 合并成功；ffmpeg 探测最终 mp3 时长 `00:20:37.70`；最终 mp3 大小 `7426460` 字节；版本搜索确认 `package.json`、`Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、`APP_VERSION` 和设计文档均为 `0.1.40`；关键文档/代码 NUL 字节检查为 0。
- 生成产物：完整音频 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260618_170200_我的文学奖_【奥地利】托马斯·伯恩哈德\audio\20260619_134333_e2e_test\我的文学奖_e2e.mp3`；分段与 SSML 位于同目录的 `parts/` 和 `ssml/`；manifest `audio_manifest.json`；绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.40_x64-setup.exe`。
- 遗留问题：本轮真实测试使用临时 Node 脚本复用后端同等 TTS/ffmpeg/飞书流程完成；后端代码已同步修复 concat 相对路径和飞书通知，后续可再通过桌面 UI 点击【音频】验证任务表状态回填。

## 22:22 说明音频通知耗时字段

- 用户要求：询问为什么收到的音频完成飞书通知没有“耗时”，而素材完成通知有。
- 执行动作：检查后端 `notify_audio_generation_completed`，确认正式应用生成的音频通知模板已经包含 `耗时：{elapsed}`；定位用户看到的通知是端到端测试中修复 ffmpeg 合并后由 PowerShell 手动补发的验证通知，手动消息漏写了 `耗时` 行，不是后端正式模板缺失。
- 结果：正式应用侧不需要改代码；后续通过桌面 UI 或后端 `generate_audio/generate_material_task_audio` 生成音频时，飞书通知会包含耗时。
- 验证方式：查看 `a-book-in-30-minutes/src-tauri/src/commands.rs` 中音频通知模板，已包含 `音频时长` 和 `耗时` 两行；临时 Node 测试脚本也包含 `耗时：${manifest.elapsedSeconds}秒`。
- 生成产物：日报说明。
- 遗留问题：无。

## 22:26 修复 foobar 显示音频时长偏短

- 用户要求：截图显示 foobar 中最终 mp3 持续时间为 `15:28`，与此前 ffmpeg 探测到的 `20:37.70` 不一致，需要确认原因。
- 执行动作：重新检查最终 mp3 和 4 个分段的 ffmpeg 探测结果；确认 4 个分段时长分别约 `5:59.98`、`6:00.58`、`5:58.42`、`2:38.74`，合计约 `20:37.72`，说明 TTS 内容没有缺失；判断 foobar 显示偏短是 MP3 分段使用 `-c copy` 无重编码拼接后，播放器读取 MP3 头/索引信息估算时长不一致导致。
- 结果：后端 ffmpeg 合并策略已从 `-c copy` 改为重新编码为单一 CBR MP3：`-ar 48000 -ac 2 -codec:a libmp3lame -b:a 192k -write_xing 1`，用于写入更兼容的时长索引并避免 foobar 显示偏短。
- 验证方式：用已生成 4 个分段重新合并修正版 mp3，ffmpeg 探测显示 `Duration: 00:20:37.73`、`bitrate: 192 kb/s`、`Audio: mp3, 48000 Hz, stereo`；`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1`、`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 均通过。
- 生成产物：修正版音频 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260618_170200_我的文学奖_【奥地利】托马斯·伯恩哈德\audio\20260619_134333_e2e_test\我的文学奖_e2e_192k_stereo.mp3`。
- 遗留问题：无。

## 22:27 提升音频输出品质

- 用户要求：指出 foobar 截图显示当前音频为 `MP3 / 48 kbps / 24000 Hz / 单声道`，品质太低。
- 执行动作：将微软语音默认输出格式从 `audio-24khz-48kbitrate-mono-mp3` 提升为 `audio-24khz-160kbitrate-mono-mp3`；本机 `settings.json` 同步写入该格式；最终合并输出统一转为 `48kHz / 192kbps / stereo`；同步更新详细设计文档；版本升至 `0.1.41` 并重新构建安装包。
- 结果：后续新生成音频的源分段码率更高，最终 mp3 不再是 `48 kbps / 24000 Hz / 单声道`，而是播放器友好的 `192 kbps / 48000 Hz / stereo`。
- 验证方式：PowerShell 调用微软语音验证 `audio-24khz-160kbitrate-mono-mp3` 可返回正常音频；修正版最终 mp3 经 ffmpeg 探测为 `192 kb/s`、`48000 Hz`、`stereo`；版本搜索确认 `package.json`、`Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、`APP_VERSION` 和设计文档均为 `0.1.41`；关键文档/代码 NUL 字节检查为 0。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.41_x64-setup.exe`。
- 遗留问题：修正版 mp3 是用已生成的 48kbps 分段重编码得到；后续通过新版正式再生成时，分段源音频也会提升到 160kbps。

## 22:35 调整半小时音频目标字数

- 用户要求：目标是生成 `30-35` 分钟音频，因此素材旁白应生成 `8000-10000` 字文本。
- 执行动作：将前端默认 `settings.materialProfile.targetMinChars/targetMaxChars` 从 `5005/6000` 改为 `8000/10000`；Rust 默认 `MaterialProfile` 同步改为 `8000/10000`；默认生成方向补充“旁白目标为 30-35 分钟，最佳约 9000 个中文字”；本机 `settings.json` 同步写入新目标；设计文档补充“目标时长应通过素材字数反推”的说明。
- 结果：默认素材生成目标已调整为 `8000-10000` 字；配置页和流水线页将默认显示新目标；后续生成素材会按该目标请求 AI，若旁白字数不达标仍会触发一次修复 prompt；版本升至 `0.1.42`。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；版本搜索确认 `package.json`、`Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、`APP_VERSION` 和设计文档均为 `0.1.42`；旧 `5005/6000` 文档说明已清理；关键文档/代码 NUL 字节检查为 0。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.42_x64-setup.exe`。
- 遗留问题：需要后续用新目标重新生成一本书，确认 AI 能稳定产出 8000-10000 字旁白；如果 AI 仍偏短，需要加强 prompt 或增加多轮扩写策略。

## 22:42 素材到音频端到端重测

- 用户要求：端到端测试【素材】到【音频】完整链路，本次先删除已有素材和音频。
- 执行动作：定位源文件 `E:\迅雷下载\0308新书四本\2025-01《我的文学奖》【豆瓣评分8.1】\2025-01《我的文学奖》【豆瓣评分8.1】.epub`；删除旧导出目录 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260618_170200_我的文学奖_【奥地利】托马斯·伯恩哈德`；使用当前素材目标 `8000-10000` 重新生成素材，再用微软语音和 ffmpeg 生成音频。
- 结果：素材生成成功，旁白中文字数 `9067`，满足 `8000-10000`；音频生成链路成功，11 个 TTS 分段均生成，最终 mp3 为 `192 kb/s`、`48000 Hz`、`stereo`；但音频时长为 `00:40:14.81`，超过目标 `30-35` 分钟，说明当前 `zh-CN-XiaochenNeural` 在 `+0%` 语速下，约 9000 字会接近 40 分钟，不符合预期。
- 验证方式：ffmpeg 探测最终 mp3：`Duration: 00:40:14.81`、`bitrate: 192 kb/s`、`Audio: mp3, 48000 Hz, stereo`；分段源音频为 `160 kb/s`、`24000 Hz`、`mono`，符合当前 Azure 源输出配置。
- 生成产物：新素材包 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260619_225307_我的文学奖_【奥地利】托马斯·伯恩哈德`；音频 `...\audio\20260619_225307_pipeline\20260619_225307_我的文学奖_【奥地利】托马斯·伯恩哈德.mp3`。
- 遗留问题：需要把默认语速提高，或收窄素材字数目标；本轮优先将默认 TTS 语速调至 `+20%`，让约 9000 字旁白落到约 33-34 分钟。

## 20:26 修复打开素材文件夹错误显示

- 用户反馈：点击【打开素材文件夹】后页面只显示 `[object Object]`，无法判断真实错误。
- 问题判断：Tauri 后端错误以 `{ message: string }` 对象返回，前端右键菜单 catch 使用 `String(caught)`，导致对象被显示为 `[object Object]`；同时旧任务曾被错误回填到其它书的素材目录，必须禁止跨书打开素材包。
- 执行动作：新增前端 `formatCommandError`，统一提取 `Error.message`、后端 `message` 字段或 JSON 文本，避免再显示 `[object Object]`；后端反查旧素材目录时只允许源文件名或书名匹配，禁止回退到其它书的最近素材包；【素材】按钮把缺少 `material_output_dir` 的已完成任务视为缺失并重新生成；清理本机 SQLite 中错误回填的 `material_output_dir`；版本升至 `0.1.37`；同步更新设计文档。
- 验证方式：待执行前端构建、Rust 检查、Tauri Windows 构建，并重启绿色版 release exe；随后按用户要求做端到端验证。
- 遗留问题：无。

## 15:09 打开生成素材文件夹

- 用户反馈：用户要打开的是生成后的素材文件所在文件夹，不是 EPUB 源文件夹。
- 问题判断：上一版只修了 `openPath` 权限和错误反馈，但菜单语义仍然指向源文件所在目录；任务表也没有持久化生成素材包目录，导致已完成任务无法直接定位 `title.txt`、`narration.txt`、`subtitles.txt` 等生成文件。
- 执行动作：新增 `material_tasks.material_output_dir` 字段并迁移旧库；素材生成成功后自动写出素材包到默认 `exports` 目录，复用现有 `title.txt`、`description.txt`、`tags.txt`、`narration.txt`、`subtitles.txt`、`draft.srt`、`prompt.txt`、`overview.json`、`materials.json`、`README.md` 格式；自动导出成功后把目录写回任务表；新增后端 `open_material_output_dir` 命令，通过资源管理器打开生成素材目录；右键【打开素材文件夹】改为调用该命令；旧任务如果没有保存 `material_output_dir`，则按源文件名在默认 `exports` 目录查找最近素材包并回填；版本升至 `0.1.34`；同步更新详细设计文档。
- 结果：新生成的素材任务会自动产生素材包目录，并在任务表中保存；右键【打开素材文件夹】会打开该生成目录，而不是源 EPUB 所在目录；旧任务也会尽量自动反查历史导出目录。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock` 到 `0.1.34`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧 `a_book_in_30_minutes.exe` 并通过 `dev-start.cmd` 启动新版 release exe；版本文件均为 `0.1.34`；日报、设计文档和关键代码 NUL 检查为 0。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.34_x64-setup.exe`。
- 遗留问题：旧任务只有在默认 `exports` 下能按源文件名匹配到素材包时才可自动回填；如果素材包被移动或改名，需要重新生成一次或后续提供手动绑定目录功能。

## 15:00 修复打开素材文件夹无响应

- 用户反馈：任务列表右键菜单点击【打开素材文件夹】没有效果，没有打开对应的素材文件夹。
- 问题判断：新增动作调用了 Tauri opener 的 `openPath(parentPath)`，但 capability 只包含 `opener:default`，该默认权限允许打开 URL 和在资源管理器中定位文件，不包含 `open_path`，所以直接打开目录会被权限拦截；同时右键动作缺少错误反馈，导致界面看起来像没有反应。
- 执行动作：新增 `opener:allow-open-path` 权限；右键菜单动作统一捕获异常并显示到页面错误区域；保留【打开文件夹】为资源管理器定位源文件，【打开素材文件夹】为直接打开源文件所在目录；版本升至 `0.1.32`；同步更新详细设计文档。
- 结果：Tauri capability 与生成后的 schema 均包含 `opener:allow-open-path`；右键菜单打开目录失败时会显示错误，不再静默无响应。
- 验证方式：`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock` 到 `0.1.32`；`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧 `a_book_in_30_minutes.exe` 并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.32_x64-setup.exe`。
- 遗留问题：当前任务表还没有持久化“导出的素材包目录”，所以【打开素材文件夹】本轮打开的是源 EPUB/TXT/PDF/DOCX 所在目录；如果后续要打开生成出的素材包，需要给任务表增加素材输出目录字段并在导出/生成时回填。

## 14:50 素材流水线跳过已完成任务

- 用户要求：如果所有素材已经生成，点击【素材】就跳过生成，不要再次生成，除非有缺失。
- 执行动作：将【素材】按钮改为“只补缺失”：已完成且有成稿字数的任务跳过；待处理、失败、生成中断或缺失成稿字数的任务才会重新生成；若全部已完成则显示“素材已全部生成，无需重复生成”；版本升至 `0.1.31`；同步更新详细设计文档。
- 结果：点击【素材】时会先筛选当前勾选任务、当前任务或任务列表中的可生成素材；如果没有缺失任务，不再调用后端生成接口，只在界面显示“素材已全部生成，无需重复生成”。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；首次直接执行 `pnpm tauri build --ci --target x86_64-pc-windows-gnu` 因未带项目 GNU 工具链环境而报 `link.exe` 不存在，改用 `RUSTUP_HOME`、`CARGO_HOME`、`RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu` 和 MSYS2 `mingw64\bin` 后，`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧 `a_book_in_30_minutes.exe` 并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.31_x64-setup.exe`。
- 遗留问题：本轮先一次点击处理第一个缺失任务，后续再扩成队列式批量串行处理。

## 14:48 右键菜单增加打开素材文件夹

- 用户要求：任务列表右键菜单增加【打开素材文件夹】。
- 用户补充：右键背景颜色要和主题颜色一致，不要用蓝色。
- 执行动作：保留原【打开文件夹】用于在资源管理器中定位当前文件；新增【打开素材文件夹】直接打开源素材所在目录本身；新增前端 `parentPath` 路径 helper；将右键菜单背景、边框、hover、分割线和禁用文字色统一为绿色深色主题；版本升至 `0.1.30`；同步更新详细设计文档。
- 验证方式：待执行前端构建、Rust 检查、Tauri Windows 构建，并重启绿色版 release exe。
- 遗留问题：无。

## 14:42 调整流水线阶段按钮位置

- 用户要求：将流水线页中的【素材】【音频】【视频】按钮换位置，从素材路径输入框右侧移动到“流水线分析面板”标题行右侧。
- 执行动作：使用 `SectionTitle` 的 action 区域承载三枚流水线阶段按钮；路径输入行只保留选择文件、定位文件夹、扫描三个小图标按钮；收紧阶段按钮宽度并补充标题 action 区域样式；版本升至 `0.1.29`；同步更新详细设计文档。
- 结果：三枚流水线阶段按钮已移动到面板标题行右侧，素材路径输入行不再被大按钮挤占。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧进程并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.29_x64-setup.exe`。
- 遗留问题：无。

## 14:31 流水线页面与任务多选列

- 用户要求：把【生成素材】改成【流水线】；第一个按钮是【素材】，第二个按钮是【音频】；任务列表增加音频列和时长列；表格最前面增加行勾选和表头全选。
- 用户补充：再加一个【视频】按钮，现阶段还没有实现逻辑。
- 执行动作：将左侧导航、页面标题和面板标题改为“流水线”；顶部主按钮拆为“素材”“音频”“视频”；任务列表增加全选/多选状态、素材状态列、音频状态列、时长列；音频按钮先要求选择任务并作为后续流水线入口预留；视频按钮作为禁用占位；版本升至 `0.1.28`；同步更新详细设计文档。
- 结果：流水线页已经显示三阶段按钮和带勾选列的任务表，任务表新增“素材、音频、时长”列。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧进程并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.28_x64-setup.exe`。
- 遗留问题：音频列本轮先显示任务级占位状态和时长 `-`，真实批量 TTS 接入后再回填音频状态与时长。

## 14:24 素材生成按步骤刷新进度

- 用户要求：生成素材时要根据实际步骤数刷新任务进度；如果有 4 个步骤，就按 4 步推进进度。
- 用户补充：飞书通知中的 `耗时：208.0 秒` 要改为更自然的显示，例如有小时显示 `X时Y分Z.Z秒`，没有小时显示 `Y分Z.Z秒`。
- 执行动作：把素材生成进度改为后端步骤事件驱动，按 `1/4 解析源书`、`2/4 请求 AI`、`3/4 整理结果和字幕`、`4/4 完成` 刷新到 `25%`、`50%`、`75%`、`100%`；前端监听后端 `material-task-progress` 事件并同步更新任务列表和 SQLite 状态；飞书通知耗时改为 `format_human_duration`，大于等于 1 小时显示 `X时Y分Z.Z秒`，大于等于 1 分钟显示 `Y分Z.Z秒`，不足 1 分钟显示 `Z.Z秒`。
- 验证方式：待执行前端构建、Rust 检查、Tauri Windows 构建，并重启绿色版 release exe。
- 遗留问题：无。

## 14:15 修复文件夹定位弹窗看不到 EPUB

- 用户反馈：点击“选择素材文件夹”后进入 Windows 文件夹选择器，只能看到文件夹，看不到当前目录里的 EPUB 文件。
- 问题判断：这是系统文件夹选择器的行为，`directory: true` 只允许选择文件夹，不会显示普通文件；用户实际需要的是“通过素材文件定位其所在文件夹”。
- 执行动作：把素材页的“定位文件夹”按钮改为打开文件选择器，并限制显示 `epub/pdf/txt/docx`；选中任一素材文件后自动扫描该文件所在目录。手动输入文件夹路径后点击扫描的能力保留；同步把“选择文件”按钮也加上相同扩展名过滤；版本升至 `0.1.26`；同步更新详细设计文档。
- 结果：点击文件夹图标时不再进入只能看文件夹的系统弹窗，而是进入能看到 EPUB/PDF/TXT/DOCX 的素材文件选择器。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧进程并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.26_x64-setup.exe`。
- 遗留问题：无。

## 11:15 缩小品牌字并过滤素材任务列表

- 用户要求：左上角品牌文字缩小 1 到 2 级；素材任务列表只处理 `epub`、`pdf`、`txt` 和 `docx`，其它格式不要显示到列表中；生成完成后任务行仍显示 `0%`，状态字段和字数要刷新；任务列表保存到 SQLite，参考 `yt-download`，需要分类，分类对应播放列表名称；截图中的分类下拉默认 3 个分类并支持新增。
- 执行动作：缩小侧边栏品牌标题和副标题字号；后端扫描阶段直接过滤非 `epub/pdf/txt/docx` 文件；新增 SQLite `material_tasks` 表，保存源文件路径、文件名、扩展名、大小、分类、状态、五档进度、成稿字数、消息和更新时间；新增 `get_material_tasks`、`update_material_task_status`、`remove_material_task`、`reset_material_tasks` 命令；扫描素材时 upsert 任务，生成开始/成功/失败时同步写库；素材页切换回来或切换分类时从数据库恢复任务列表；修复生成状态写入逻辑，使用最新 Zustand 工作台状态更新当前文件，避免 stale closure 导致完成后仍显示 `0%`；配置页把“频道名”升级为“分类 / 播放列表”，默认 `半小时听完一本书`、`睡前听完一本书`、`A Book in 30 Minutes`，并支持输入新增分类。
- 结果：任务列表现在只显示支持的四类文件；每个任务的分类、状态、百分比、成稿字数会持久化到 SQLite；生成完成后行内会刷新为“已完成 / 100% / 成稿字数”；失败会刷新为“失败 / 0% / 错误消息”。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`cargo fmt` 因当前 `stable-x86_64-pc-windows-gnu` 工具链未安装 `rustfmt` 组件而跳过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；已杀掉旧 `a_book_in_30_minutes.exe` 并通过 `dev-start.cmd` 启动新版 release exe。
- 生成产物：绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.25_x64-setup.exe`。
- 遗留问题：`pdf/docx` 当前只进入任务列表，正文解析仍待后续接入。

## 08:39 开发阶段改用热部署或绿色版验证

- 用户要求：当前不是正式发版阶段，仍在开发阶段；每次新版验证都卸载再安装太麻烦，希望改用热部署、热升级或绿色版方式升级。
- 执行动作：确认 Tauri 开发阶段可使用 `pnpm tauri dev` 做前端热更新，也可直接运行 release 目录下的 `a_book_in_30_minutes.exe` 作为绿色版验证；后续日常验证优先采用热更新或绿色版覆盖，只有明确要求安装包或阶段性发版时再构建 NSIS 安装包。
- 用户补充：同意该方案，并要求在桌面创建一个“开发版”快捷方式。
- 执行动作：新增 `a-book-in-30-minutes/dev-start.cmd`，封装 `pnpm tauri dev` 和当前工作区 Rust/MSYS2 环境变量；在桌面创建 `A Book in 30 Minutes 开发版.lnk`，指向该启动脚本。
- 结果：桌面快捷方式已创建，后续双击即可启动 Tauri 开发模式。
- 验证方式：确认快捷方式文件存在于 `C:\Users\Administrator\Desktop\A Book in 30 Minutes 开发版.lnk`。
- 用户纠正：需要的是可执行的最新版本，不是 `tauri dev`；启动前要先杀掉以前的进程。
- 执行动作：停止旧安装版 `a_book_in_30_minutes.exe` 和当前 `tauri dev` 相关的 `node/vite` 进程；重写 `dev-start.cmd`，启动前清理旧 `a_book_in_30_minutes.exe` 及本项目 node/pnpm/cargo/vite 进程，然后直接运行 `src-tauri\target\x86_64-pc-windows-gnu\release\a_book_in_30_minutes.exe`；更新桌面快捷方式描述。
- 结果：桌面“开发版”快捷方式现在指向绿色版最新 release exe 启动器，不再启动 `pnpm tauri dev`。
- 验证方式：执行快捷方式目标后确认进程 `a_book_in_30_minutes.exe` 来自 `D:\04_GitHub\world-cup-issue\a-book-in-30-minutes\src-tauri\target\x86_64-pc-windows-gnu\release\a_book_in_30_minutes.exe`。
- 遗留问题：如需固定绿色版目录，可后续新增一键同步脚本，把 release exe 复制到指定目录并重启。

## 08:30 素材生成页改为任务列表优先并补右键菜单

- 用户要求：截图中红框里的大介绍卡、输入卡和结果占位区域不需要；页面布局参考 `yt-download`，最重要的是任务列表；任务列表布局和右键菜单尽可能参考；菜单状态显示必须全部是中文。
- 执行动作：移除顶部大介绍卡和右侧大结果占位卡，将素材生成页重排为上方“素材分析面板”和下方“素材任务列表”；任务列表保留任务、格式、状态、进度、成稿字数、文件大小等列；新增右键菜单，参考 `yt-download` 提供继续生成、批量继续生成、打开文件、打开文件夹、取消所选任务、从列表中移除、清理状态等操作；双语字幕和同步类菜单先置为禁用预留项；同步更新详细设计文档。
- 结果：版本升至 `0.1.24`，素材生成页以任务列表为核心。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.24_x64-setup.exe`。
- 遗留问题：音频生成、同步、删除源文件等动作暂未接入真实批处理流程，菜单先作为预留能力或提示。

## 08:16 简化素材生成页并迁移生成参数到配置

- 用户要求：【素材生成】参考 `D:\04_GitHub\yt-download` 的下载中心页面，把频道名（下拉）、目标语言、最少字数、最多字数、生成方向放到【配置】菜单；生成菜单只保留输入框，可选择 epub 或把 epub 路径粘贴进输入框后直接开始处理。
- 用户补充：每个文件生成的文件需要一列标识是否成功生成，最终生成的文字数目也要有一列显示；参考页里的状态和百分比值得拿过来；百分比只使用 `0%`、`25%`、`50%`、`75%`、`100%` 五档；状态显示值必须全部是中文。
- 执行动作：新增素材生成默认配置模型 `materialProfile`，将频道名、目标语言、字数范围和生成方向迁移到 `settings`；配置页新增“素材生成默认配置”面板，频道名和目标语言使用下拉；素材生成页移除这些参数输入，只保留文件路径输入、选择文件/定位文件夹/扫描按钮、生成按钮和结果区；生成请求从配置读取默认参数；素材文件列表改为表格，新增“状态”“进度”“成稿字数”列；界面状态统一为中文，进度只显示五档百分比；同步更新详细设计文档。
- 结果：版本升至 `0.1.23`，素材页更接近 `yt-download` 的任务式入口，配置和生成职责拆开。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.23_x64-setup.exe`。
- 遗留问题：待确认频道名下拉是否需要后续支持用户自定义新增频道模板。

## 07:36 修复日报编码并补齐语音语言需求记录

- 用户要求：中文普通话应包含 29 个官方音色；其他语言类似。当前只需要中文和英文，以后可能增加日文、韩文、法语、德语；最好再增加一个下拉列表。
- 执行动作：发现日报文件存在一个 NUL 空字节，清理时 PowerShell 命令误触发空写入，已立即重建本日日报关键记录，确保 UTF-8 文本可继续维护；补齐 `zh-CN` 29 个音色种子；新增“语音语言”下拉，当前支持 `zh-CN`、`en-US`、`en-GB`；音色列表按所选 locale 从 SQLite 读取；新增 `speechProfile.locale` 并让 SSML 的 `xml:lang` 使用该字段；设置合并逻辑改为深度合并 `regionKeys`，避免保存时丢失各区域 Key；同步更新详细设计文档。
- 结果：版本升至 `0.1.22`，中文普通话音色补齐为 29 个，英文音色可通过语言下拉切换查看和试听。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.22_x64-setup.exe`。
- 遗留问题：日文、韩文、法语、德语先预留扩展路径，后续按微软官方列表继续补种子数据。

## 00:43 微软官方 TTS 语音列表写入 SQLite

- 用户要求：根据微软官方语言和语音支持页面，把文本转语音的语音列表数据写入 SQLite。
- 执行动作：新增 `speech_voices` 表，保存区域设置、语言、语音类型、音色名称、性别、风格、角色和来源 URL；应用启动时自动写入中文普通话 `zh-CN` 常用 Neural 音色种子数据；新增 `get_speech_voices` 后端命令和前端 API；配置页的人声音色下拉改为从 SQLite 查询，不再依赖前端硬编码数组；同步更新详细设计文档。
- 结果：版本升至 `0.1.21`，设置页会从 SQLite 读取微软语音音色列表，并显示微软官方来源链接。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.21_x64-setup.exe`。
- 遗留问题：本轮先内置中文普通话常用官方音色种子数据；全量官方列表在线同步可作为后续增强。

## 00:27 修复播放试听音频加载失败

- 用户反馈：点击“播放试听”时报错 `Failed to load because no supported source was found`。
- 问题判断：后端已生成 mp3 文件，但前端通过 `convertFileSrc(filePath)` 播放本地文件时，WebView 没有把该路径识别为可播放音频源。
- 执行动作：让后端试听接口返回 `data:audio/mpeg;base64,...`，前端优先播放 data URL，同时仍保留本地 mp3 文件路径用于排查；新增 `base64` 依赖。
- 结果：版本升至 `0.1.20`，播放试听不再依赖 WebView 对本地 mp3 路径的识别。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.20_x64-setup.exe`。

## 00:19 MyDiskTreeSize 扩展排除规则并继续 Z 盘入库验证

- 用户要求：确认需要继续扩展默认排除规则，把 `mysql-test` 等测试/依赖噪声目录排除，目标仍是扫完 `Z:\` 并写入 SQLite。
- 执行计划：默认排除项增加 `mysql-test`、`__pycache__`、`.pytest_cache`、`.mypy_cache`、`.venv`、`venv`、`bower_components`、`.gradle` 等目录；旧配置加载时自动合并新增默认排除项；重新构建并运行隐藏 CLI 端到端扫描 `Z:\`，检查 `scan_runs`、`scan_entries` 和 `truncated` 标记。
- 用户补充：后面需要略过的目录可以由助手自行判断，但必须标注出来，方便用户后续手工调整位置或排除规则。
- 调整计划：被自动略过的目录不再完全消失，而是在树和 SQLite 明细中保留节点，标记为“已跳过”并写入跳过原因；继续保留“深度上限”标记。

## 00:02 微软语音默认配置保存与音色试听

- 用户要求：新增播放按钮试听各种音色；新增保存按钮保存各个区域的 Key；如果某区域已有 Key，切换区域下拉时自动读取对应 Key；试听区要有可修改的默认文字；截图中的区域、人声音色、Speech Key、输出格式、语速、音调都要可以设置并保存到 SQLite，生成语音时使用。
- 执行动作：新增 `speech_region_keys` SQLite 表，按区域保存 Speech Key、人声音色、输出格式、语速和音调；配置页新增“保存默认语音配置”按钮、试听文字输入框和“播放试听”按钮；切换区域时读取 SQLite 中对应默认配置并回填；试听音频使用当前输入框文字，生成音频使用当前默认语音配置。
- 结果：版本升至 `0.1.19`；截图中的区域、人声音色、Speech Key、输出格式、语速、音调均可设置并保存到 SQLite。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`cargo update -p a_book_in_30_minutes` 已执行；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.19_x64-setup.exe`。
- 遗留问题：尚未在本轮用真实 Speech Key 逐个音色试听；需要用户安装新版后在配置页保存并试听。

## 07:37 MyDiskTreeSize 修正版本号
- 用户要求：MyDiskTreeSize 版本不要和其他 app 混起来；之前是 .1.19，本次应为 .1.20，不是 .1.21。
- 执行动作：准备将 my-disk-tree-size 内应用版本、前端展示版本、Tauri 配置和 Cargo 包版本统一回退到 .1.20，并重新构建 Windows 安装包。
- 验证方式：计划执行版本号搜索、pnpm build、cargo check、乱码扫描和 Windows 打包。
## 07:42 MyDiskTreeSize 版本号修正完成
- 修正结果：已将 MyDiskTreeSize 从误用的 .1.21 统一改回本次应发布版本 .1.20。
- 涉及文件：package.json、src-tauri/Cargo.toml、src-tauri/Cargo.lock、src-tauri/tauri.conf.json、src/config/app.ts、src/store/useAppStore.ts、src/services/diskApi.ts。
- 验证方式：pnpm build 通过；cargo check --target x86_64-pc-windows-gnu --jobs 1 通过；Windows NSIS 打包通过；乱码扫描无命中；版本搜索确认 MyDiskTreeSize 相关版本均为 .1.20。
- 生成产物：my-disk-tree-size\src-tauri\target\x86_64-pc-windows-gnu\release\bundle\nsis\MyDiskTreeSize_0.1.20_x64-setup.exe，大小 7168637 字节。
- 说明：.1.21 安装包是误打包产物，不作为本次有效版本使用。

## 08:23 MyDiskTreeSize SQLite 路径说明
- 用户要求：询问 SQLite 文件路径，准备手工查看。
- 回答内容：说明端到端验证生成的临时 SQLite 路径，以及应用正式运行时默认 app.db 所在位置。

## 00:12 素材到音频端到端重测收尾

- 用户要求：端到端测试【素材】到【音频】，本次先删除已有素材和音频，并确认音频时长、品质符合 30-35 分钟目标。
- 执行动作：延续本轮已完成的真实链路测试结果，确认旧导出目录已删除后重新从《我的文学奖》EPUB 生成素材；素材目标从 `5005-6000` 调整为 `8000-10000` 字并同步本机设置；微软语音输出从 `audio-24khz-48kbitrate-mono-mp3` 提升到 `audio-24khz-160kbitrate-mono-mp3`；ffmpeg 合并从 `-c copy` 改为重编码输出 `48kHz / 192kbps / stereo`，修复 foobar 时长误判和单声道低码率问题；默认 TTS 语速调整为 `+20%`，并把版本升至 `0.1.43`。
- 结果：新素材旁白中文字数 `9067`，满足 `8000-10000` 目标；`+0%` 语速音频为 `00:40:14.81`，超过目标；使用 `+20%` 重新合成同一文本后，最终音频时长为 `00:33:31.25`，落入 `30-35` 分钟目标，输出品质为 `192 kb/s / 48000 Hz / stereo`。
- 验证方式：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过；ffmpeg 探测最终 mp3 显示 `Duration: 00:33:31.25`、`bitrate: 192 kb/s`、`Audio: mp3, 48000 Hz, stereo`；版本和配置搜索确认 `0.1.43`、`8000/10000`、`+20%`、`audio-24khz-160kbitrate-mono-mp3` 已同步到代码与设计文档；关键代码和文档 NUL 字节检查均为 0；飞书补发最终达标版通知，机器人返回 `code=0/msg=success`。
- 生成产物：新素材目录 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260619_225307_我的文学奖_【奥地利】托马斯·伯恩哈德`；目标达标音频 `C:\Users\Administrator\AppData\Roaming\com.abookin30minutes.desktop\exports\20260619_225307_我的文学奖_【奥地利】托马斯·伯恩哈德\audio\20260619_233415_rate20\rate20_192k_stereo.mp3`；绿色版 exe `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/a_book_in_30_minutes.exe`；安装包 `a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.43_x64-setup.exe`。
- 遗留问题：无。
