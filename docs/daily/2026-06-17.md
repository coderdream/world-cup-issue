# 2026-06-17 日报

## 倒计时与手动刷新交互修复

- 用户要求：下一场倒计时要实时减少；`上次更新` 左侧刷新按钮必须可点击并拉取最新数据。
- 执行范围：今日概览、比赛时间解析、前端状态管理、浏览器预览兜底逻辑。
- 关键操作：新增北京时间比赛时间解析；增加每秒刷新的倒计时逻辑；将刷新图标改为按钮并接入 `refreshMatches`；补齐浏览器预览下 `refresh_matches/get_matches` 的本地兜底逻辑。
- 测试结果：`pnpm build` 通过；`cargo check --target x86_64-pc-windows-gnu` 通过；本地预览中倒计时可逐秒变化，点击刷新后 `上次更新` 时间会变化。
- 遗留风险：小交互已验证；若交付安装版需继续升版本并重新打包。

## 托盘图标、项目身份、版本与更新流程

- 用户要求：托盘图标不能是白色；后续每次修改都要升补丁版本、重新打包，并让 `检查更新` 可发现新版本；项目名称为 `WorldCupIssue（世界杯组手）`。
- 执行范围：Tauri 应用身份、图标资源、托盘、更新器、打包脚本、发布清单、仓库规则。
- 关键操作：更新仓库规则；版本升至 `0.1.1`；保持可执行文件 `world_cup_issue.exe` 和标识符 `com.worldcupissue.app`；重新生成绿色/金色奖杯图标；托盘显式加载 `icons/32x32.png` 并关闭模板图标模式；新增更新服务、签名密钥、`scripts/package-windows.ps1`、`scripts/write-update-manifest.mjs`。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check`、`scripts/package-windows.ps1` 均通过；安装包元数据为 `ProductName=WorldCupIssue`，版本为 `0.1.1`。
- 产物：`WorldCupIssue_0.1.1_x64-setup.exe`、签名文件 `.sig`、`release/0.1.1/latest.json`。
- 遗留风险：需要上传到 GitHub 发布页后，远程更新器才能发现该版本。

## 概览页、比分页与公开 CupWatch 文档对齐

- 用户要求：按参考截图修正概览和比分界面，并参考公开 CupWatch 官网与功能文档。
- 执行范围：概览页、比分页、左侧导航、状态推导、公开文档行为。
- 关键操作：参考公开页面，不接触闭源实现；增加 30 秒全局自动刷新；增加比分导航红色实时数量徽标；增加 `Ctrl+Alt+C` 热键路由；赛程页增加阶段筛选、手动刷新、实时行高亮；比分、积分榜、淘汰赛增加手动刷新入口。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check` 均通过；本地预览显示版本 `0.1.3`、比分徽标 `1`、概览聚焦实时比赛。
- 产物：版本升至 `0.1.3`；生成 `WorldCupIssue_0.1.3_x64-setup.exe`；发布 GitHub 发布页 `v0.1.3`；更新清单可通过代理验证。
- 遗留风险：球队详情侧栏、AI 比赛选择器、模型配置等细节仍待继续完善。

## 当前时间驱动的数据刷新与比赛状态

- 用户要求：必须结合当前时间刷新数据，避免比赛状态、倒计时、比分页停留在静态赛程。
- 当前观察：北京时间 `2026-06-17 09:23:36` 附近，`09:00` 开球的 Argentina vs Algeria 应视为进行中，除非可信数据源已给出完场比分。
- 关键操作：通过代理验证公开数据可获取；新增前端 `src/domain/matches.ts` 与 Rust 侧 `src-tauri/src/match_status.rs`；前后端统一从北京时间开球时间推导 `live/finished/scheduled`；将归一化接入页面初始化、手动刷新、进程间命令和公开数据解析；概览优先展示实时比赛；比分页 `进行中` 标签页渲染实时卡片；选择可用源中比分更完整的数据，避免陈旧镜像覆盖新比分。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check` 均通过；本地预览中概览卡显示 `正在进行`，比分页显示 `全部 24 / 进行中 1 / 已结束 18 / 未开始 5`。
- 遗留风险：公开数据不保证实时，赛中真实比分仍依赖 football-data.org 令牌或其它可信增量源。

## Argentina vs Algeria 比分与 football-data 令牌

- 用户要求：实际比分为 Argentina 2:0 Algeria，但应用显示 0:0 或空白；提供 football-data.org 文档；设置页 `API Token` 必须保存。
- 执行范围：公开源核验、football-data 增量源、设置持久化、比分合并、界面展示。
- 关键操作：核查公开源，确认部分数据滞后或缺失；规划令牌存储与后端刷新客户端；保持字段名为 `API Token`；调整未知比分展示，避免把未知误导为确定 `0:0`。
- 遗留风险：公开源仍可能滞后；真实赛中比分需有效 football-data 令牌支撑。

## 0.1.4 修复、内存问题与 0.1.5 发布

- 用户要求：继续推进、确认是否发新版、解释为什么截图仍是旧版本和错误状态。
- 问题原因：`0.1.4` 修复期间出现编码损坏和 Windows 内存耗尽；旧安装版仍停留 `0.1.4`；无最终比分比赛在 120 分钟后被错误标为结束。
- 关键操作：修复兜底数据、更新模拟文案、Tauri 配置、Rust 样本和更新说明；清理卡住进程与缓存；将无最终比分比赛的进行中/待定窗口延长到 180 分钟；同步修复 TypeScript 与 Rust 状态逻辑；发布 `v0.1.5`。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；GitHub 更新接口通过代理返回 `0.1.5`。
- 产物：`WorldCupIssue_0.1.5_x64-setup.exe`，GitHub 发布页 `v0.1.5`。
- 遗留风险：部分公开源仍缺真实赛果，赛中比分需令牌。

## 上次更新时间语义修复

- 用户要求：`上次更新` 必须在真正拿到新数据后才更新；刷新失败要记日志；刷新时间显示整分钟。
- 关键操作：修改刷新命令合约，只有成功从公开源拉取并缓存后才返回新的 `lastUpdated`；网络失败或仅使用缓存/模拟数据时不推进更新时间；增加 Rust 警告日志；展示格式改为北京时间 `HH:mm`。
- 结果：合入后续 `0.1.7` 发布。
- 遗留风险：日志持久化布局后续在框架抽取阶段继续完善。

## 令牌测试与 AI 模型配置

- 用户要求：`API Token` 后加 `测试Token`；实现 AI 模型配置；支持兼容 OpenAI 的接口测试；支持复制当前配置为 `ai.profile` JSON。
- 执行范围：设置页、AI 配置弹窗、令牌测试、兼容 OpenAI 的接口测试、导入导出、发布。
- 关键操作：新增 AI 配置类型与 `src/domain/aiConfig.ts`；新增 Tauri 命令 `test_football_data_token` 和 `test_ai_model_config`；令牌测试调用 football-data；AI 测试向 `/chat/completions` 发送 `你好，请只回复 ok`；设置页新增 `测试Token`；实现模型配置弹窗、复制分享、粘贴自动填充、保存和清除；修复 `ai.profile` 导入映射。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check` 均通过；更新接口返回 `0.1.7` 和后续 `0.1.8`。
- 产物：GitHub 发布页 `v0.1.7`、`v0.1.8`；安装包 `WorldCupIssue_0.1.7_x64-setup.exe`、`WorldCupIssue_0.1.8_x64-setup.exe`。
- 遗留风险：GitHub 最新下载地址可能有短时缓存，但版本和安装包地址正确。

## AI 评估生成流程与按钮转圈修复

- 用户要求：AI 配置已成功后，点击 `生成 AI 评估` 不应再弹配置窗；令牌测试成功后按钮不应一直转。
- 关键操作：将令牌测试与后续比赛刷新拆开，测试成功立即停止加载状态，刷新在后台执行；`生成 AI 评估` 在配置完整时直接调用兼容 OpenAI 的接口，仅缺字段时打开配置；增加 AI 评估请求、接口、本地兜底、Rust 命令、提示词、结果面板、加载与错误状态。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check`、`scripts/package-windows.ps1` 均通过；更新接口返回 `0.1.9`。
- 产物：GitHub 发布页 `v0.1.9`；安装包 `WorldCupIssue_0.1.9_x64-setup.exe`。
- 结果：令牌测试加载状态和 AI 生成入口均修复并发布。

## 中文安装包、字体、AI 布局和窗口拖拽

- 用户要求：安装程序改成中文；AI 分析页优化成参考图样式；字体比 CupWatch 大，需要调小；顶部栏可拖拽窗口。
- 关键操作：NSIS 切换简体中文，确认脚本包含 `SimpChinese.nsh` 和 `MUI_LANGUAGE "SimpChinese"`；降低整体字号，收紧导航和标题；顶部栏增加拖拽并排除按钮、输入框等交互元素；用本地 Edge/Playwright 截图指标验证。
- 测试结果：`pnpm build`、`pnpm docs:build`、`cargo check`、`scripts/package-windows.ps1` 均通过；更新接口返回 `0.1.10` 和 `0.1.11`。
- 产物：GitHub 发布页 `v0.1.10`、`v0.1.11`；安装包 `WorldCupIssue_0.1.10_x64-setup.exe`、`WorldCupIssue_0.1.11_x64-setup.exe`。
- 遗留风险：视觉仍需继续按截图细抠，但字体、拖拽和安装器语言已完成。

## 抽取 Tauri 框架

- 用户要求：把框架抽取到 `tauri-framework`，包含首页、配置页和关于页，并尽可能框架化。
- 执行范围：从主应用抽取可复用 Tauri/React 桌面壳，不带 WorldCupIssue 业务页面。
- 关键操作：创建独立模板工程；前端使用 Vite + React + TypeScript + Zustand；后端使用 Tauri 2；保留托盘、关闭到托盘、自定义窗口按钮、顶部栏拖拽；提供首页、配置页、关于页；提供简化接口服务；配置中文 NSIS；初始进程间命令包含 `get_app_state/get_settings/set_settings/check_update_mock`；验证后清理生成缓存。
- 测试结果：`pnpm install`、`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1` 均通过。
- 结果：框架抽取完成，目录为 `tauri-framework`。

## 框架安装包打包测试

- 用户要求：打包抽取出的 `tauri-framework` 供安装试用。
- 关键操作：移除框架模板中不需要的更新器配置和依赖；移除残留 `createUpdaterArtifacts`；版本升至 `0.1.4`；同步 Rust 锁文件；执行框架打包脚本。
- 测试结果：`pnpm install`、`cargo update -p tauri_framework`、`scripts/package-windows.ps1` 均通过。
- 产物：`tauri-framework/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/Tauri Framework_0.1.4_x64-setup.exe`，大小 `5,972,711` 字节。
- 遗留风险：框架内部部分中文资源仍有编码问题，后续需统一清理。
