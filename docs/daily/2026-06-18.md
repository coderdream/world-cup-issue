# 2026-06-18 日报

## 22:31 MyDiskTreeSize 扫描心跳与 Z 盘端到端验证

- 用户要求：从操作日志看扫描停在 `Z:\docker_data`，感觉卡死；需要修改防止无进度假死，并端到端扫描完整 `Z:\`，最终存入 SQLite。
- 初步判断：当前后端只在子目录扫描开始和结束时写日志；如果某个一级目录很大、NAS 响应慢或某个目录项读取耗时很长，日志页会长时间没有新内容，用户会误判为卡死。
- 执行计划：为后台扫描增加心跳日志和扫描计数进度；必要时降低 UI 更新压力；完成构建后对 `Z:\` 做真实端到端扫描，查询 SQLite 的 `scan_runs` 与 `scan_entries` 验证批次、文件数、目录数和总大小。
- 端到端观察：新版隐藏 CLI 扫描 `Z:\` 时日志已每 5 秒持续写入，进程保持响应；但完整扫描卡在 `.pnpm-store`、`node_modules`、`hexo` 等海量小文件缓存目录，23 分钟后仍在推进且尚未写入最终 `scan_runs`。
- 调整决定：用户确认需要排除这些高噪声缓存目录；后续改为默认跳过 `node_modules`、`.pnpm-store`、`.git`、`.cache` 等目录，并在日志中记录跳过项，再重新端到端验证。
- 用户补充：深度超过 `8` 的目录不要继续扫描，但需要在结果里标记出来。

## 15:25 提交并推送代码

- 用户要求：提交并推送代码。
- 执行动作：检查 Git 状态、暂存、提交并推送。
- 结果：已创建提交 `46b193b`，提交信息为 `feat: implement WorldCupIssue app and reusable Tauri framework`，并推送到 `origin/main`。
- 遗留问题：提交后仍有 `a-book-in-30-minutes` 子项目的功能改动留在工作区，后续单独处理。

## 16:35 修正 A Book in 30 Minutes 素材输入和模型配置

- 用户要求：素材文件选择不要在浏览阶段过滤；切换菜单后素材页状态不丢；最大化后页面自动扩展；AI 测试成功后保存 Key；右上角显示模型名；用指定 EPUB 做端到端生成验证。
- 执行动作：素材输入新增“选择文件”和“定位文件夹”；目录扫描显示所有普通文件，解析阶段再判断 `EPUB/TXT/DOCX/PDF`；将素材页请求、扫描结果、生成结果、导出目录、错误提示、当前任务 ID 等迁移到全局 store；修正页面宽度；AI 测试前后保存配置；右上角新增模型名。
- 结果：版本升至 `0.1.14`，安装包生成成功。
- 验证方式：`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1`、`scripts/package-windows.ps1` 通过；使用用户提供 EPUB 端到端生成素材成功。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.14_x64-setup.exe`。
- 遗留问题：本机未配置飞书 Webhook，真实生成未发送飞书通知。

## 17:13 增强操作日志详细程度

- 用户要求：日志界面显示 `debug`、`info` 等详细日志；查看本次生成的 log，而不是直接看文本 log 文件。
- 执行动作：为 `operate_log` 增加 `trace_id`；后端日志支持 `DEBUG/INFO/WARN/ERROR` 和任务 ID；生成、解析、AI 请求、JSON 解析、字幕切分、导出、飞书通知等链路增加详细日志；日志页默认按当前或最近一次素材生成任务过滤。
- 结果：版本升至 `0.1.15`，日志页可显示本次任务详细过程。
- 验证方式：`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1`、`scripts/package-windows.ps1` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.15_x64-setup.exe`。

## 17:50 修复 EPUB 解析卡住并补充设计文档

- 用户要求：EPUB 解析几分钟不应让用户干等；有错误要抛出；新增详细设计文档，每次更新都要同步维护。
- 执行动作：定位并修复 Rust `regex` 不支持 `</\1>` 反向引用导致的 EPUB HTML 清洗异常；为源书解析增加 panic 兜底和 30 秒超时；为 AI 和飞书请求增加超时；新增 `docs/a-book-in-30-minutes-design.md`，补录产品定位、页面结构、后端命令、数据存储、源书解析、Prompt、导出、日志、飞书通知、版本打包和历史设计。
- 结果：版本升至 `0.1.16`，解析异常会返回界面并写入本次任务日志。
- 验证方式：`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1`、`scripts/package-windows.ps1` 通过；`cargo fmt` 因固定工具链缺少 `rustfmt` 未执行；`cargo test` 在 Windows GNU debug 链接阶段报 `export ordinal too large`，未进入测试逻辑。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.16_x64-setup.exe`。

## 19:35 同步操作日志能力到 Tauri Framework

- 用户要求：把写日志的相关逻辑及代码提交到 `tauri-framework` 框架里，后续派生应用可复用。
- 执行动作：将 `OperationLogger`、SQLite `operate_log`、`trace_id`、`get_operation_logs`、日志页、搜索、选择复制、清空显示等能力同步到 `tauri-framework`；更新 `docs/tauri-framework-design.md`；框架版本升至 `0.1.11`。
- 结果：框架具备通用操作日志后台和 IDEA 风格日志页。
- 验证方式：`pnpm build`、`cargo check --target x86_64-pc-windows-gnu --jobs 1`、`scripts/package-windows.ps1` 通过。
- 生成产物：`tauri-framework/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/Tauri Framework_0.1.11_x64-setup.exe`。
- 遗留问题：Tauri 打包继续提示 `com.tauriframework.app` 以 `.app` 结尾不推荐，本轮未调整 identifier。

## 22:26 A Book in 30 Minutes 新增生成音频菜单

- 用户要求：参考 `D:\04_GitHub\video-easy-creator` 和 `D:\04_GitHub\yt-download`，新增“生成音频”菜单；配置中设置微软语音；准备好 `ffmpeg.exe`，但不要打包进安装包，而是配置路径。
- 参考结论：`video-easy-creator` 主要采用 Azure/Microsoft Speech + SSML；`yt-download` 的工具使用方式是把 `ffmpeg` 作为可配置外部工具路径并在执行前校验。
- 执行动作：新增 `speechProfile` 和 `toolProfile` 设置；配置页新增“微软语音配置”和“工具路径”；新增 `test_speech_profile`、`test_ffmpeg_path`、`generate_audio` 后端命令；新增“生成音频”菜单和 `AudioPage`；音频页支持使用素材旁白或手动输入，长文本按句分段，每段调用微软语音 REST TTS，多个分段时使用配置的外部 `ffmpeg.exe` 拼接最终 mp3；日志记录音频生成计划、微软语音请求、ffmpeg 拼接和耗时，不记录 Speech Key 明文。
- 结果：版本升至 `0.1.17`；设计文档已同步补充音频生成、微软语音配置、ffmpeg 外部路径、日志动作和默认输出目录。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；在子项目目录执行 `pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。曾误从子项目调用仓库根目录 `scripts/package-windows.ps1`，该脚本会回到根目录打主应用并两次超时，已改用子项目 Tauri build 完成正确打包。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.17_x64-setup.exe`。
- 遗留问题：未使用真实微软语音 Key 做端到端音频合成；需要用户在配置页填写 Speech Key、区域、音色和 ffmpeg 路径后测试。

## 23:42 补充微软语音 Key 获取入口

- 用户要求：说明“微软语音 Key”在哪里可以找到，最好在 app 里加一个可点击链接。
- 执行动作：在配置页“微软语音配置”中增加 Azure Portal 语音资源入口、官方 REST TTS 文档入口和简短说明；区域改为中文+英文+region code 下拉；人声音色改为听书常用中文音色下拉；同步更新设计文档、版本号和安装包。
- 结果：版本升至 `0.1.18`，用户可直接从 App 打开 Azure 语音资源页面，进入 Speech 资源后在 `Keys and Endpoint` 查看 Key 和区域。
- 验证方式：`pnpm build` 通过；`cargo update -p a_book_in_30_minutes` 已同步 `Cargo.lock`；`cargo check --target x86_64-pc-windows-gnu --jobs 1` 通过；`pnpm tauri build --ci --target x86_64-pc-windows-gnu` 通过。
- 生成产物：`a-book-in-30-minutes/src-tauri/target/x86_64-pc-windows-gnu/release/bundle/nsis/A Book in 30 Minutes_0.1.18_x64-setup.exe`。
- 遗留问题：音色列表当前是内置常用中文音色；后续可接微软 voices/list API 按区域在线拉取完整音色列表。
